services:
  - type: web
    name: efficientnet-webapp
    env: python
    buildCommand: "python download_model.py"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
